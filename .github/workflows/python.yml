name: Python

on:
  push:
    branches: [master, main]
    paths:
      - 'crates/liq-ta-python/**'
      - 'crates/liq-ta/**'
      - '.github/workflows/python.yml'
  pull_request:
    branches: [master, main]
    paths:
      - 'crates/liq-ta-python/**'
      - 'crates/liq-ta/**'
      - '.github/workflows/python.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  PYO3_USE_ABI3_FORWARD_COMPATIBILITY: 1

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.12', '3.13', '3.14']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/liq-ta-python

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install maturin pytest numpy

      - name: Build and install package
        working-directory: crates/liq-ta-python
        run: |
          maturin build --release --out dist
          python -m pip install --no-index --find-links dist liq-ta

      - name: Run tests
        working-directory: ${{ runner.temp }}
        run: python -m pytest "${{ github.workspace }}/crates/liq-ta-python/tests" -v

  build-wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/liq-ta-python

      - name: Install maturin
        run: pip install maturin

      - name: Build wheel
        working-directory: crates/liq-ta-python
        run: maturin build --release

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: crates/liq-ta-python/target/wheels/*.whl

  # Build wheels for multiple Python versions and platforms
  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install maturin
        run: pip install maturin

      - name: Build sdist
        working-directory: crates/liq-ta-python
        run: maturin sdist

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: crates/liq-ta-python/target/wheels/*.tar.gz

  # Optional: publish to PyPI (requires secrets configuration)
  # publish:
  #   name: Publish to PyPI
  #   needs: [test, build-wheels, build-sdist]
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         pattern: wheels-*
  #         merge-multiple: true
  #         path: dist
  #
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: sdist
  #         path: dist
  #
  #     - name: Publish to PyPI
  #       uses: pypa/gh-action-pypi-publish@release/v1
  #       with:
  #         password: ${{ secrets.PYPI_API_TOKEN }}
