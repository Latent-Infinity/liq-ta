[package]
name = "liq-ta"
description = "High-performance technical analysis library for financial time series"
autobenches = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords = ["technical-analysis", "trading", "finance", "indicators", "stocks"]
categories = ["science", "algorithms", "mathematics"]
readme = "../../README.md"

# Include only essential files in published crate (excludes tests and fixtures)
include = [
    "src/**/*",
    "examples/**/*",
    "benches/**/*",
    "Cargo.toml",
    "../../LICENSE",
    "../../README.md",
]

[features]
default = []
serde = ["dep:serde"]
# Parallel batch processing using Rayon
parallel = ["dep:rayon"]
# Debug feature: treat TA-Lib reference divergences as test failures instead of warnings
reference-checks-strict = []
# Enable dhat allocation profiling tests (run separately: cargo test --features dhat-heap)
dhat-heap = ["dep:dhat"]
# Use Fast precision mode by default (native-type accumulators for f32 inputs)
# When not enabled, High precision mode is used (f64 accumulators for f32 inputs)
precision-fast = []

[dependencies]
num-traits = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, optional = true }
rayon = { workspace = true, optional = true }
dhat = { version = "0.3", optional = true }

[dev-dependencies]
rand = { workspace = true }
rand_chacha = { workspace = true }
proptest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "indicators"
harness = false

[[bench]]
name = "workload"
harness = false

[[bench]]
name = "simd_comparison"
harness = false

[[bench]]
name = "precision_baseline"
harness = false

[[bench]]
name = "precision_comparison"
harness = false
